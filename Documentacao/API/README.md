# API REST — Contrato e uso

Documentos desta pasta:

| Arquivo | Descrição |
|---------|-----------|
| [rotas.md](./rotas.md) | Todas as rotas: método, path, parâmetros, body, respostas, o que está implementado e o que falta. |
| [exemplos-teste.md](./exemplos-teste.md) | Comandos `curl` e exemplos de request/response para testar cada endpoint. |

O fluxo completo do usuário (busca texto/planilha → seleção → gerar cotação → editar → estatísticas → relatório) está em [../FluxoUsuario.md](../FluxoUsuario.md).

Base URL assumida nos exemplos: `http://localhost:3001` (configurável via `PORT`).
